---
# handlers file for nginx
#  handlers:
    - name: restart php-fpm
      systemd:
        daemon_reload: yes
        name: php-fpm
        state: restarted
        enabled: yes

    - name: reload php-fpm
      systemd:
        name: php-fpm
        state: reloaded